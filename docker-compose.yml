version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: blockguard
      POSTGRES_PASSWORD: blockguard
      POSTGRES_DB: blockguard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
